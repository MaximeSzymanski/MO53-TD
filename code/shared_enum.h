#ifndef SHARED_ENUM_H
#define SHARED_ENUM_H

namespace shared_enum{
    enum input {TEXT, BIN};
    enum storage {LIST, VECTOR};
    enum output {TEXT, BIN, SCREEN};
    enum calculation {MOVING_AVG, SORT, AVG, SUM, COUNT};
}

#endif